[project]
name = "governed-analytics-workflow"
version = "2.0.1"
description = "Reasoning-first, evidence-governed analytics workflow skill"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.unittest]
start-directory = "tests"
